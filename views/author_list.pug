extends layout

block content
  h1= title

  if author_list.length
    ul
      each author in author_list
        li
          a(href=author.url) #{author.name}
          |  (#{author.birth_due_back_formatted} - #{author.death_due_back_formatted})
  else
    p There are no authors.